import "vks-compiler/lexer.vks";

function main() {
  let source: str = "function main() {}";
  let tokens: Token = tokenize(source);
  
  let curr: Token = tokens;
  while (curr != null) {
    print(curr.type + " : '" + curr.lexeme + "'");
    curr = curr.next;
  }
}
